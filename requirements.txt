# Зависимости «Садового помощника».
#
# Ставится одной строкой:  python -m pip install -r requirements.txt
#
# Из внешнего программе нужен ровно один пакет: связь с браузером по
# отладочному протоколу идёт по вебсокету. Ни Selenium, ни Playwright не
# берём — они тяжелее и тянут в сборку лишнее.
websocket-client>=1.7

# Только для разработки: проверки и сборка .exe.
pytest>=8
pyinstaller>=6
