# ── Сборка ───────────────────────────────────────────────────────────
build/
dist/
*.spec

# ── ЧУЖОЕ: сюда нельзя ───────────────────────────────────────────────
# `reference/` держит исходники САМОЙ ИГРЫ (upjers) и код других авторов —
# расширение CupIvan 5.2 и Anbauhelfer. Они нужны для работы над проектом,
# но прав распространять их у нас нет. Разбор игры опубликован своими
# словами в `docs/game-api.md` — там объяснения и короткие цитаты, а не копия.
reference/

# ── Рабочая память ───────────────────────────────────────────────────
# Внутренний дневник проекта: решения, ошибки, журнал заходов. Полезен в
# работе, но это не документация и не для публики.
MEMORY.md

# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
